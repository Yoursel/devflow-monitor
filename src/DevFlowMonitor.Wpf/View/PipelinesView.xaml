<UserControl x:Class="DevFlowMonitor.Wpf.View.PipelinesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:DevFlowMonitor.Wpf.ViewModel"
             xmlns:dto="clr-namespace:DevFlowMonitor.Wpf.Dto"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModel:PipelinesListViewModel}">
    <Grid Style="{StaticResource PageRoot}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource PageTitle}">Pipelines</TextBlock>

        <Button Grid.Row="0" Width="120" HorizontalAlignment="Right" Style="{StaticResource MainButton}" Margin="0 0 10 10">
            <Button.Content>
                <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="{StaticResource AddIcon}"/>
                    <TextBlock>Добавить</TextBlock>
                </WrapPanel>
            </Button.Content>
        </Button>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Style="{StaticResource SectionContent}">
            <ItemsControl ItemsSource="{Binding Pipelines, UpdateSourceTrigger=PropertyChanged}"
                          HorizontalAlignment="Stretch">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0 10 0 0">
           

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center">

            <Button Style="{StaticResource SecondaryButton}"
                    Command="{Binding Pagination.GoToFirstPageCommand}"
                    Width="35">
                <Image Source="{StaticResource FirstPageIcon}" Margin="0"/>
            </Button>

            <Button Style="{StaticResource SecondaryButton}"
                    Command="{Binding Pagination.PreviousPageSetCommand}"
                    Width="35">
                <Image Source="{StaticResource PreviousPageIcon}" Margin="0"/>
            </Button>

            <ItemsControl ItemsSource="{Binding Pagination.VisiblePages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type dto:PageInfo}">
                        <Button Width="35"
                                Content="{Binding Number}"
                                Command="{Binding DataContext.Pagination.GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding Number}">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Background" Value="#A2EFB7"/>
                                            <Setter Property="Foreground" Value="#1A4A2E"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Style="{StaticResource SecondaryButton}"
                    Command="{Binding Pagination.NextPageSetCommand}"
                    Width="35">
                <Image Source="{StaticResource NextPageIcon}" Margin="0"/>
            </Button>

            <Button Style="{StaticResource SecondaryButton}"
                    Command="{Binding Pagination.GoToLastPageCommand}"
                    Width="35">
                <Image Source="{StaticResource LastPageIcon}" Margin="0"/>
            </Button>

 
            </StackPanel>

            <TextBlock
                  Text="{Binding Pagination.ItemRangeInfo, UpdateSourceTrigger=PropertyChanged}"
           HorizontalAlignment="Right"
           Foreground="{StaticResource MutedForeground}"
           FontSize="13"/>
        </Grid>
    </Grid>
</UserControl>